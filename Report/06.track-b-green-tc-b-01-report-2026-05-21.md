# Step 06 Report — Track B GREEN (TC-B-01)

| 항목 | 내용 |
|------|------|
| **단계** | 06 — track-b-green-tc-b-01 |
| **워크플로** | 리팩토링 우선 (TDD GREEN만 · REFACTOR 금지) |
| **일자** | 2026-05-21 |
| **브랜치** | green |

---

## 1. 수행 작업

1. **RED 확인** — `entity::UnitConverter` 구현 `.cpp` 부재 → `unit_logic_red_tests` **링크 RED** (`undefined reference`)
2. **비율 상수 추출** — `src/entity/LengthConversionRatios.hpp`
   - `kFeetPerMeter = 3.28084` (PRD §3.2)
   - `kYardPerMeter = 1.09361` (TC-B-02용 예비, 미사용)
3. **최소 GREEN** — `src/entity/UnitConverter.cpp`
   - `convert`: **meter → feet** 분기만 (`value * kFeetPerMeter`)
   - `convertAll` / `registerUnit`: 링크용 스텁 (빈 벡터 / no-op)
4. **CMake** — `UnitConverter.cpp`를 `unit_entity` 라이브러리에 추가
5. **ConfigLoader** — `loadConfig()` 정의 추가 (빈 카탈로그 반환 스텁, TC-B-05/06 미구현)
6. **Export** — Report/06·Prompt/06·full-transcript 갱신

**금지 준수:** REFACTOR 없음 · TC-B-02~06·TC-A 동시 GREEN 없음 · 비율 인라인 하드코딩 없음

**테스트 위치:** 사용자 지시 `tests/test_converter.cpp`는 없음. TC-B-01 실제 소스는 `tests/unit/entity/logic_track_red_tests.cpp` (`UnitConverter::convert`).

---

## 2. 변경 파일

| 경로 | 상태 |
|------|------|
| `src/entity/LengthConversionRatios.hpp` | 신규 |
| `src/entity/UnitConverter.cpp` | 신규 |
| `CMakeLists.txt` | 수정 (`UnitConverter.cpp` 추가) |
| `src/data/ConfigLoader.cpp` | 수정 (`loadConfig` 스텁) |
| `Report/06.track-b-green-tc-b-01-report-2026-05-21.md` | 신규 |
| `Prompt/06.track-b-green-tc-b-01-transcript-2026-05-21_prompt.md` | 신규 |
| `Prompt/full-transcript-refactor-first-2026-05-21_prompt.md` | 갱신 |

**미변경(의도):** `logic_track_red_tests.cpp`, `UnitConverter.hpp`, `LengthConversionEngine`, Track A 소스

---

## 3. 검증 결과

| 검증 | 결과 | 비고 |
|------|------|------|
| **build (CMake)** | ⚠️ **부분 성공** | 5/6 타깃 링크 OK; `unit_ui_red_tests` 링크 RED 유지 (`ConversionUseCase` 미구현) |
| **ctest** | ⚠️ **4/6 PASS** | Step 03 스위트 4개 PASS; `unit_logic_red_tests` 1/6 TC PASS; `unit_ui_red_tests` Not Run |
| **Catch2 필터 TC-B-01** | ✅ | `unit_logic_red_tests.exe "[TC-B-01]"` — 1 assertion PASS |
| **Catch2 필터 TC-B-02** | ✅ (의도적 FAIL) | `0.0` vs `2.734025` — 미구현 확인 |
| **수동 (기존 Engine)** | ✅ | `unit_conversion_tests` ctest PASS (Step 03 GREEN 유지) |

### build 요약

```
cmake -S . -B build && cmake --build build
→ unit_logic_red_tests.exe ✅
→ unit_ui_red_tests.exe ❌ (undefined reference: ConversionUseCase::*)
```

### ctest 요약

```
ctest (build/)
→ unit_conversion_tests, unit_boundary_tests, unit_catalog_tests, unit_config_tests: PASS
→ unit_logic_red_tests: FAILED (TC-B-01 PASS, TC-B-02~06 FAIL — GREEN 범위 외)
→ unit_ui_red_tests: Not Run (실행 파일 없음)
```

### TC-B-01 수동 필터

```
.\build\unit_logic_red_tests.exe "[TC-B-01]"
→ All tests passed (1 assertion in 1 test case)
```

---

## 4. DEFECT / Invariant

| ID | 상태 |
|----|------|
| **DEF-001** (meter→feet, `UnitConverter` 경로) | **부분 해소** — Track B RED 스위트 TC-B-01만 GREEN |
| **DEF-001** (`LengthConversionEngine` 경로) | Step 03에서 이미 GREEN |
| **INV-D01** | TC-B-01에서 `kFeetPerMeter` 상수로 검증 |

---

## 5. 다음 단계 권고

1. **GREEN 순차** — TC-B-02 (meter→yard) → TC-B-03 (`convertAll`) → TC-B-04 (`registerUnit`) → TC-B-05/06 (`loadConfig` 실구현)
2. **허브 통합** — 분기 나열 대신 `LengthConversionEngine` + `UnitCatalog` 위임 (REFACTOR 단계에서)
3. **Track A GREEN** — `ConversionUseCase.cpp`·`OutputFormatter` (TC-A-01~06, 링크 RED 해소)
4. **ctest 정합** — Logic 스위트 필터 태그 또는 `ctest -R`로 TC별 게이트 분리 검토
5. **README / defect_list** — DEF-001 Track B 항목 `[x]` 부분 반영(다음 Export 시)

---

## 6. 커밋 메시지 (권고)

```
feat(green): implement convert meter to feet (Approx 1e-5)
```
